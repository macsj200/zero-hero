[project]
name = "zero-hero"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.10.0",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
